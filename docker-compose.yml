services:
  journallatex:
    build: .
    image: journallatex
    user: "${USER_ID:-1000}:${GROUP_ID:-1000}"
    volumes:
      - .:/data:Z
    working_dir: /data
    environment:
      - HOME=/tmp
    command: make